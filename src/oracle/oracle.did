type endpoint = record {
    url: text;
    resolver: text;
}

service : {
    get_address: () -> (variant { Ok: text; Err: text });
    setup: (vec endpoint, nat64, nat64, text) -> (variant { Ok: null; Err: text });
    stop_fetcher: () -> (text);
    subscribe: (text, text) -> (text);
}
